version: '3.8'

tasks:
  docs:
    desc: Serve the ./doc on localhost:8080
    cmds:
      - go run docs.go
  editor:
    desc: Serve the test editor on localhost:8080
    cmds:
      - go run ./playground/.