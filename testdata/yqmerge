input:
    base.yml: |-
        --- #export/base
        backend:
            host: foo
            port: 443
            probe:
                url: /default
                timeout: 60s
                threshold: 2
        --- #export/probe
        probe:
            url: /override
        ---
        !yq .base.backend *? .probe
output: {}
